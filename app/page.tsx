import {Breadcrumb, BreadcrumbItem, BreadcrumbLink, BreadcrumbList} from "@/components/ui/breadcrumb";

export default function Home() {
    return (
        <main>
            <h1 className={ "absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 font-bold text-2xl animate-pulse" }>Work in progress...</h1>
        </main>
    );
}
